<h1>@ViewBag.Wedinfo.WedOne's & @ViewBag.Wedinfo.WedTwo's Wedding</h1>
<a href="/Dashboard">Dashboard</a> | <a href="/logout">Logout</a>
<h3>Date/Time of Wedding:</h3>
<ul>
    <li>@ViewBag.Wedinfo.WedDate</li>
</ul>
<div class="twocolumn">
    <h3># of RSVP's:</h3>
    <ul>
        <li>@ViewBag.ListGuest.WedtoUser.Count</li>
    </ul>
    <h3>Guest List</h3>
    <ul>
        @foreach(var i in ViewBag.ListGuest.WedtoUser){
        <li>
            @i.User.FirstName @i.User.LastName
        </li>
        }
    </ul>
    <h3>Wedding Address:</h3>
    <ul>
        <li>@ViewBag.Wedinfo.WedAddress</li>
    </ul>
</div>
<br>
<div class="twocolumn">

    <div id="dvMap" style="height:250px;"></div>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
    <script type="text/javascript"
        src="https://maps.googleapis.com/maps/api/js?libraries=places&key=PersonalGoogleAPIRemovedForSecurityPurposes&callback=initMap"
        async defer></script>
    <script type="text/javascript">
        function initMap() {
            var map = new google.maps.Map(document.getElementById('dvMap'), {
                center: {
                    lat: @ViewBag.Lati,
                    lng: @ViewBag.Long
        },
        zoom: 15
    });
        var marker = new google.maps.Marker({
            position: {
                lat: @ViewBag.Lati,
        lng: @ViewBag.Long
        },
        map: map,
            draggable: true
    });
        google.maps.event.addListener(marker, 'dragend', function () {
            var Lats = marker.getPosition().lat();
            var Longs = marker.getPosition().lng();
            $('#Lat').val(Lats);
            $('#Long').val(Longs);
        });
}
    </script>

</div>